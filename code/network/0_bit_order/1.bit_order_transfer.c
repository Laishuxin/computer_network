#include <arpa/inet.h>
#include <stdio.h>
#include <stdlib.h>
#include <strings.h>

void create_socket() {
    struct sockaddr_in saddr;
    bzero(&saddr, sizeof(struct sockaddr_in));
}

int main() {
    return 0;
}
